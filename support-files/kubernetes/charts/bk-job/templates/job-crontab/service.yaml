{{- if eq (include "job.crontab.service.enabled" .) "true" }}
apiVersion: v1
kind: Service
metadata:
  name: job-crontab
  namespace: {{ .Release.Namespace }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: "job-crontab"
    {{ include "job.labelKeys.jobScope" . }}: {{ include "job.labelValues.jobScope.backend" . }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.crontabConfig.service.type }}
  ports:
    - name: http
      port: {{ .Values.crontabConfig.service.port }}
      targetPort: {{ .Values.crontabConfig.containerPort }}
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ include "common.names.name" . }}
    app.kubernetes.io/component: "job-crontab"
{{- end }}
