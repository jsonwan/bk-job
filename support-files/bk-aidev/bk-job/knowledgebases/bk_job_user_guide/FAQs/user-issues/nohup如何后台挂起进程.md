# nohup 如何后台挂起进程

## 现象描述

使用 `nohup command &` 命令后台挂起进程后，立刻查询会发现进程已经不在；如果直接在机器上执行，进程则会一直存在。

## 问题定位及处理

如果 `nohup` 启动的程序的父进程 ID 是 JOB 的任务 ID，那么该程序会随任务一起退出。

将命令修改为下述形式即可解决：

> **说明**：原文在此处未给出修改后的具体命令内容，建议补充后再完善本篇。
