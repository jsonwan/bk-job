# python 脚本如何引用全局变量

## Q：python 脚本如何引用全局变量

A：通过脚本参数传递。目前只有 shell 脚本支持直接引用全局变量，其他语言不支持。

## Q：那如何在 python 里修改全局变量

A：非 shell 脚本里面目前修改不了变量。可以把变量输出到文件，再加一个步骤用 shell 读取文件并设置变量。

## Q：如何申明 python 的执行路径呢，比如我想 python 执行的路径为 /tmp

A：这个路径是固定的，在 `/tmp/bkjob` 下面。脚本使用的 python 解释器可以通过首行 shebang 指定。
