# 全局变量 id 获取方式

## 现象描述

如何获取全局变量的 id？

## 问题定位及处理

1. 全局变量传参可以采用 `name` 或 `id` 的方式指定，推荐使用 `name`。
2. 全局变量可以在作业平台的执行方案详情页面查看。`id` 一般通过 ESB 接口获取；也可以打开作业平台中需要查找全局变量 `id` 的执行方案，然后按 F12 打开开发者工具，在 Network 请求响应里查找。
