# 魔法变量使用

## 问题描述

1. 获取机器自己的 IP 和 hostname。
2. 使用其他主机获取第一个步骤中的 IP 和 hostname。

## 参考方案

获取某个命名空间变量的汇聚值：

```bash
# job_import {{JOB_NAMESPACE_host_name}}
```

```bash
echo ${JOB_NAMESPACE_host_name}
```

赋值：`host_name=$(hostname)`。

**注意**：`#` 和 `job_import` 之间的空格不能省略。
