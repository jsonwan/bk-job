# JOB 同步目录是全量同步还是按文件 MD5 增量同步

## 现象描述

JOB 同步目录时是全量同步，还是会检查文件 MD5 进行增量同步？

## 问题定位及处理

如果目标文件的位置相同、名称相同、内容也相同，则不会重复传输。

如果使用目录打包模式，会先打包再传输，每次生成的都是新的压缩包，因此不会做增量同步。

如果确实需要增量同步，推荐使用标准运维编排作业，组合以下步骤实现：先用脚本执行 `ls` 列出目录下的所有文件，得到文件列表，再通过标准运维的参数传递能力，把该文件列表传递给下一个文件传输任务。
