# 分发文件提示成功，但是机器上文件没更新

## 问题描述

在 JOB 上分发文件，任务提示成功，但是目标机器上的文件没有更新。

## 排查步骤

文件传输最后是要做校验的，成功之后就表示签名校验已通过；文件有变化却显示成功，不会出现"没有传过去"的情况。

- 对比源文件与目标文件的 MD5，确认两个同命名文件是否是同一个文件。
- 查看 agent 版本是否为最新，1.0 有类似的问题，2.0 后已修复。
- 更换目标目录尝试分发文件，确认目标目录或源文件目录权限是否有问题。
- 检查参数是否有多余字符，比如文件名、目录名参数中有多余的空格。
